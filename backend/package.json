{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.12.1"
  },
  "scripts": {
  "start": "concurrently \"yarn backend\" \"yarn frontend\" \"yarn electron\"",
  "backend": "cd ../backend && yarn start",
  "frontend": "cd ../frontend && yarn start",
  "electron": "yarn run electron ."
}
,

  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
